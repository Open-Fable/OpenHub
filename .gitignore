# Dependencies
node_modules/

# Upstream app repos — cloned at runtime by scripts/setup.sh
# Never commit these; they are independent upstream projects
apps/

# Build output
dist/
out/
*.dmg
*.zip
*.app

# Electron
release/

# Environment (secrets NEVER on disk — use Keychain)
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# TypeScript build cache
*.tsbuildinfo

# Runtime config templates (user-specific copies, not the shared templates)
config/openhub-settings.json
config/openhub-projects.json
config/openhub-memory.json
config/selected-models.json
config/opencode.local.json

# Build artifacts
build/

# Vendored Node toolchain — downloaded at build time by scripts (afterPack/setup),
# multi-MB binaries that must never enter version control. Regenerated on demand.
vendor/

# Scratch / temp / recovery scripts
scratch/
salvage*.cjs
recover_*.cjs
check_keys.js
test_syntax.py
docs/audits/SECURITY_AND_BUGS_REPORT.md

# Recovery-script OUTPUT files — these contain dumped user chat history and must
# never be committed (the scripts above are ignored, but their outputs were not).
recovered_*.json
recovered_chats_*.json
recovered_history.json
salvaged_*.json
salvaged_history.json
salvaged_messages.json
salvaged_regex.json
test_script_*.js

# Unrelated scratch workspaces / screenshots / external projects accidentally
# placed at the repo root — keep them out of OpenHub.
fi/
oui/
pokjh/
interface/
interface-chat/
Portfolio_d_Art/

# Orchestrator test-output workspaces (each has a WORKSPACE_INDEX.md) and debug
# screenshots — generated during development, never part of the shell.
bouteil/
bouteil2/
code-pure/
csv/
saas/
saas-screen/
svc-screen/
cqui/
ghj/
pied/
sport/
sport-screen/
teste/
teste 2/

# Internal audit / handoff / open-defect docs — working notes, not for distribution.
docs/audits/AUDIT_RECAP_*.md
docs/audits/ORCHESTRATEUR_*.md
docs/audits/SESSION-HANDOFF.md
reports/

# Graphify output (regenerated on demand, user-specific)
graphify-out/

# Brand design files (working documents, user-specific)
fichier-de-la-marque/

# User-specific memory / personal data (never share between users)
/AGENT-MEMORY.md
/MEMORY-SYSTEM.md
/openhub-projects.html

# AI agent config (user-specific)
.claude/
.codex/
.agents/

# Internal security/audit reports and scratch dirs — working docs, never distribute
docs/audits/SECURITY_AUDIT_*.md
avertissement/
manga/
screen/
